import asyncio
import cherry_indexer as c


asyncio

async def main():
    c.
    config = c.Config(
        project_name="iceberg_example",
        description="Iceberg example",
        pipelines={
            "my_pipeline": c.
        }
    ) 

asyncio.run(main())
